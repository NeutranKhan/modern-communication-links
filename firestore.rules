rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through validated Next.js server APIs. Admin SDK bypasses rules.
    match /{document=**} { allow read, write: if false; }
  }
}